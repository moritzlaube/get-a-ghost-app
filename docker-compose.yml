version: '3'
services:
  app:
    image: get-a-ghost-app
    build:
      context: .
      dockerfile: dockerfile
    environment:
      - MONGODB_REMOTE_URI=mongodb+srv://moritz:cl5rdrcJtGzeKg0a@cluster0.dg6v9.mongodb.net/get-a-ghost?retryWrites=true&w=majority
    ports:
      - 3000:3000
      - 35729:35729
    volumes:
      - ./src:/app/src
